name: hook_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.0
  google_sign_in: ^6.2.1
  jwt_decoder: ^2.0.1
  image_picker: ^1.1.0
  shimmer: ^3.0.0
  uuid: ^4.5.1
  web_socket_channel: ^2.4.0
  flutter_secure_storage: ^9.2.4
  flutter_dotenv: ^6.0.0
  intl: ^0.19.0

  http_parser: any
  google_fonts: ^6.3.2
  google_maps_flutter: ^2.5.0
  google_maps_flutter_web: ^0.5.4+2
  geolocator: ^10.1.0
  permission_handler: ^11.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # To include images, uncomment and modify this:
  # assets:
  #   - assets/images/logo.png
